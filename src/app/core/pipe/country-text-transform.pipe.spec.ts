import { CountryTextTransformPipe } from './country-text-transform.pipe';

describe('CountryTextTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryTextTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
