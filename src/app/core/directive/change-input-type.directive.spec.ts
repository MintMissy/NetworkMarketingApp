import { ChangeInputTypeDirective } from './change-input-type.directive';

describe('ChangeInputTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeInputTypeDirective();
    expect(directive).toBeTruthy();
  });
});
