import { InvalidInputIconDirective } from './invalid-input-icon.directive';

describe('InvalidInputIconDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidInputIconDirective();
    expect(directive).toBeTruthy();
  });
});
